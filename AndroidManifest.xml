<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.scottagarman.android.dreamdrawer"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- SDK MIN -->
    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15"/> <!-- target 8 for 2.2, 7 for 2.1 -->

    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:label="@string/app_name"
                 android:hardwareAccelerated="true"
                 android:theme="@style/Theme.DreamDrawer.ActionBar"
                 android:name="DreamDrawerApplication"
            >
        <activity android:name=".activities.LauncherActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.SignUpActivity"/>
        <activity android:name=".activities.LoginActivity"/>

    </application>
</manifest> 
